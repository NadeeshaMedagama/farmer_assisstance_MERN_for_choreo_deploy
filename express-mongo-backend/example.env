# Server
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000
LOG_LEVEL=info

# Database Configuration
# Choose ONE of the following database connections:

# Option 1: Local MongoDB (for development)
MONGODB_URI=mongodb://localhost:27017/farmer-assistance

# Option 2: Cloud MongoDB Atlas (for production)
# MONGO_URI=mongodb+srv://nadeeshamedagama:Nadeesha2001@cluster0.aairnvz.mongodb.net/FarmerBackend?retryWrites=true&w=majority&appName=Cluster0

# Note: MONGODB_URI takes priority over MONGO_URI
# To use cloud database, comment out MONGODB_URI and uncomment MONGO_URI

# Auth
JWT_SECRET=supersecretjwt
JWT_EXPIRE=30d
OIDC_ISSUER=https://your-issuer.example.com
OIDC_AUDIENCE=your-api-audience-or-client-id
OIDC_LOGOUT_REDIRECT=http://localhost:3000

# HTTPS (optional for local dev)
HTTPS_ENABLE=false
SSL_KEY_PATH=certs/key.pem
SSL_CERT_PATH=certs/cert.pem

# Email SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_app_password
SMTP_FROM="Farmer Assistance <no-reply@example.com>"

# External APIs
WEATHER_API_KEY=316ac904c3c082b761a5b8d0795f894d
MARKET_API_URL=

# Twilio (optional)
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=
